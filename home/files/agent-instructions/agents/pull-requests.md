# Pull request creation

- Obtain the user's explicit approval after the pull request details are finalized and immediately before creating it.
- Approval is not required when the base repository owner is `runbear-io` or `isac322`.
- When the base repository owner is `runbear-io`, request `runbear-bot` as a reviewer and assign `isac322`.
